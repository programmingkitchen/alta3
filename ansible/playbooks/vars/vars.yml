repo_url: "https://github.com/example/example-repo.git"
repo_dest: "/tmp/example-repo"

# Branch used for checkout/create, pull, and push.
branch_name: "feature/update-files"

# Branch used when initially cloning the repository.
default_branch: "main"

remote_name: "origin"
files_to_stage: "."
commit_message: "Update files from Ansible automation"

git_user_name: "Your Name"
git_user_email: "you@example.com"
