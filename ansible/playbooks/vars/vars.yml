repo_url: "git@github.com:programmingkitchen/alta3.git"
repo_dest: "/home/student/test/"
repo_parent_dir: "/home/student/test/"

# Branch used for checkout/create, pull, and push.
branch_name: "feature/update-files"

# Branch used when initially cloning the repository.
default_branch: "main"

remote_name: "origin"
files_to_stage: "."
commit_message: "Update files from Ansible automation"

# Directory copy variables used by copy-dir.yml
source_dir: "/tmp/source_dir"
dest_dir: "/tmp/target_dir"
